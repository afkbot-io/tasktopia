# Карта и графика: 16 замечаний от 8 сентября 2026

Реализация по прямому запросу пользователя «Начни реализацию». Контекст страны — Дедлайново (`4201d605-751a-469b-8d5f-a7e322a7ece1`). База изменений — `5d3d45ca`, ветка `codex/visual-consistency-16`. Проверки проводились на отдельной локальной тестовой стране; данные Дедлайново не изменялись.

## Что изменено

| № | Результат |
|---|---|
| 1 | Машины, люди и животные размещаются на целых экранных пикселях; масштаб микроспрайтов кратный, повороты переключают готовые кадры. |
| 2 | В движении восемь типов машин: четыре исходных, пикап, микроавтобус, хетчбэк и доставка. |
| 3 | Восемь видов животных получили по четыре направления. Статические кадры удалены из runtime. |
| 4, 11 | Светофоры ставятся только у дороги на безопасном сухом участке, включая край тротуара. Удалены дубли и визуальные стойки прямых переходов; логика пропуска транспорта сохранена. Фонари сохраняют существующий редкий шаг размещения. |
| 5 | В просмотре задачи нет панели переноса в другой спринт. |
| 6 | Миниатюры городов сохраняют размер при зуме страны и планеты. На стране они кэшируются отдельно от ландшафта, рисуются без сглаживания и перемещаются по целым пикселям. Позиции аэропортов, концы авиарейсов и подходы ЖД учитывают этот масштаб. |
| 7 | Четыре новых облака с квадратными цветовыми группами; звёзды выровнены по пиксельной сетке и согласованы с палитрой. |
| 8 | Круглый силуэт планеты образован целыми квадратными ячейками со ступенчатым краем. |
| 9 | Вода покрывает весь экран страны. Детализация материалов страны и планеты одинакова: 2×2 фрагмента на географическую ячейку. |
| 10 | Камера допускает горизонтальное перетаскивание и когда карта меньше окна. |
| 12 | Узкие наземные участки озеленяются с дорожками на любой стадии, без строительной рамки. Неиспользованные узкие площадки не получают разметку стройки. Новые планы кварталов не создают участки уже трёх клеток; остатки остаются озеленением. Существующие планы и задачи сохраняют геометрию, идентичность и доступность. |
| 13 | В версии 1.24.0 табличка квартала показывала количество задач. По следующему запросу заменена диапазоном min–max, включая пропуски (см. [доработку](tasks/map-loading-geography.md)); номер участка — номер задачи. Добавлена легенда цветов и значков; проверка исправления обозначается песочными часами вместо галочки завершения. |
| 14 | Новая верхняя панель: расположение, поиск, план и обозначения. Нижняя навигация: Планета / Страна / Город / Районы. «Город» выключает границы, «Районы» включает. |
| 15 | Старые каменные props не выводятся, в том числе в сохранённых сценах. Четыре новых лодки с горизонтальным и вертикальным видом. |
| 16 | Проверены действующие компактные аэропорт, клиника, пожарная часть и пиксельные эффекты инцидентов. Эти здания уже были в новой стилистике и сохранены. Сравнение — `screenshots/visual-consistency/services.png`. |

## Проверки

- `npm run lint`, `npm run build` (включает TypeScript) — успешно, Node 24.
- Затронутые модульные проверки: камера, движение, направления, микроспрайты, размещение света, посадки, подписи, материалы и каталог. Отдельно проверено фактическое появление всех восьми типов машин.
- `scripts/audit-pixel-city-pack.py`: 35 семейств / 175 стадий зданий, 110 props, 76 микроспрайтов, 429 PNG; нет отсутствующих ссылок, лишних PNG и нарушений палитры.
- `scripts/verify-micro-ambient.py`: 76 спрайтов, корректные исходники, альфа, размеры и хеши.
- `tests/e2e/visual-consistency.spec.ts`: Chromium, 1440×900 и 390×844; переключение районов, горизонтальный drag города/страны, отсутствие переноса, легенда, заполнение воды, ступенчатая планета, отсутствие горизонтального overflow и ошибок страниц/HTTP. Тест ожидает окончания перехода до drag.
- Ревью выполнено основным агентом. Исправлен обнаруженный старый список четырёх машин в генераторе движения и обновлены затронутые селекторы прежних E2E-сценариев.

Полные длительные симуляции и прежние специализированные многогородские E2E-фикстуры не запускались. Дополнительная проверка на 150 городах и сценарий инцидентов описаны ниже. Сценарии этих фикстур обновлены под новые кнопки; это не считается их выполнением. Продакшен не выкладывался. Серверные контракты, схема данных и существующие назначения зданий не изменены.

## Воспроизведение графики

Исходники находятся в `assets/pixel-city-pack/reference/ai-authored/micro-ambient-v1/sources`. Они созданы ImageGen, затем обработаны штатной детерминированной нормализацией: удаление фона, ограниченная палитра, ближайший пиксель и точные четвертьобороты лодок.

```sh
.venv-assets/bin/python scripts/build-micro-ambient.py --build
.venv-assets/bin/python scripts/build-visual-refresh.py
.venv-assets/bin/python scripts/audit-pixel-city-pack.py
.venv-assets/bin/python scripts/verify-micro-ambient.py
```

Порядок важен: второй publisher обновляет общий манифест, атлас props и ревизию после обновления микроспрайтов. Итоговая ревизия графики — `2c6ae552240c0384`.


## Завершающий проход

- Чтение Дедлайново через MCP: один город, 14 задач, среди текущих задач нет аэропорта или вокзала. Это объясняет отсутствие рейсов. Данные страны не менялись; транспорт проверен на отдельной фикстуре, а не выдан за снимок Дедлайново.
- Новые планы кварталов проверены на всех строительных шаблонах, четырёх направлениях упаковки и восьми seed: ширина и высота каждого нового участка не меньше трёх клеток. Старые v2/v3 планы продолжают читаться без перепланировки.
- Камера: фиксированы размеры миниатюр и взаимные расстояния внутри города, подписи и область перехода учитывают новый размер. Географические якоря перемещаются вместе с картой. Миниатюры ограничены 96 пикселями по длинной стороне плюс поля; невидимые за экраном скрываются. Центры городов планеты предварительно вычисляются, поиск объектов выполняется через Map.
- Chromium и WebKit: зум с измерением размеров, drag, 150 городов и поиск в каталоге, полёты, ЖД, движение кораблей, меню/легенда/просмотр задачи, мобильная ширина. Снимки — `screenshots/atlas-transport/*-chromium.png` и `*-webkit-visual.png`.
- Жизненный цикл инцидента проверен на отдельной локальной БД: переход через доменный сервис, изменение типа через HTTP, появление огня/воды и удаление эффектов. Статус, прогресс и тип задач восстанавливаются в finally. Снимки — `screenshots/visual-consistency/services-*.png`.
- Состояние проверок последнего прохода: 67 модульных тестов затронутого набора, дополнительные HTTP-проверки переноса, восемь браузерных сценариев (четыре в каждом движке), TypeScript, сборка и линтер — успешно. На карте со 150 городами максимум обработки кадра камеры: Chromium 1,5 мс, WebKit 3 мс. Это время обработчика камеры, не время вывода полного кадра на экран.

Полноэкранная проверка исходных мест на живой Дедлайново после выкладки остаётся проверкой релиза: локальные снимки показывают тестовые данные. Merge и выкладка в этот запрос не входят.
