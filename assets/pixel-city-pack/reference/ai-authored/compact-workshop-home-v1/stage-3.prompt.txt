STRICT LOCAL EDIT of this exact stage4 house. Keep EVERY exterior pixel, all facade windows and doorway, colors, frame and groundline, source canvas and margins exactly unchanged. Change ONLY inside the roof rectangle: remove remaining LEFT roof slab, replace with opaque dark interior floor matching the right half, add TWO beige axis-aligned room partitions joining the existing right rooms, with gaps for passages. All wall edges screen-horizontal or screen-vertical, absolutely no receding side wall or trapezoid. NO ROOF whatsoever, no roof equipment. Two small square ochre material stacks inside; a FEW patchy brick marks only inside existing facade wall fields if necessary. Stage3 is roughly half assembled by lack of roof,finish,glazing, never by shrinkage. One-storey native48x24 pixel sprite, coarse uniform square pixel clusters, muted cartoon palette; no blurry texture, no antialiasing, no outside objects. Flat magenta background.
