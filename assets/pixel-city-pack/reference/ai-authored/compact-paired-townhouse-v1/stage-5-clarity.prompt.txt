STRICT localized pixel-art edit. Preserve the exact same source canvas, magenta background, outer silhouette, two roof colors, roof boundary, balcony, entrance and two very short facade floors. This image is ultimately sampled to only 48x30 pixels. The window detail currently DISAPPEARS at that size. Replace every tiny checkerboard window (facade and roof) with a SOLID broad deep teal rectangle, no mullions or subpixel noise: each facade opening visually corresponds to 3x2 native pixels (about 65x44 source pixels); each door a solid deep teal4x3 native rectangle, no internal grid. The central vertical stair glazing remains a solid deep teal strip. Match locations, do NOT alter the camera, facade height or building bounds. Deliberate coarse rectangular limited-palette Game Boy style, no realistic textures, no dark foundation border. Output only one finished stage5 house on the identical magenta canvas.
